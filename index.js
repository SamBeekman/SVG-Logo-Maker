// code to initiate the application, use either 'node index.js' or 'npm start' from the terminal window or 'npm run test' to run the tests.
const CLI = require('./lib/cli');

const newCLI = new CLI();

newCLI.run();